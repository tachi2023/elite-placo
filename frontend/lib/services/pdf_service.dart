/// Export PDF de la fiche de métrage et des situations financières
/// (Module 5 — pdf + printing packages déjà déclarés dans pubspec.yaml).
class PdfService {
  Future<String> genererFicheMetrage(/* FicheMetrage fiche */) async {
    // TODO Phase 6 : construire le document avec le package `pdf`,
    // l'enregistrer via path_provider, retourner le chemin du fichier.
    throw UnimplementedError('À implémenter en Phase 6');
  }
}
